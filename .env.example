# Copy to .env and fill in. .env is git-ignored and its values are redacted
# from every evidence artifact parity-gate writes.
#
# Suites reference credentials by name only, never by value:
#     auth = "env:PARITY_BASELINE_TOKEN"

PARITY_BASELINE_TOKEN=
PARITY_CANDIDATE_TOKEN=
